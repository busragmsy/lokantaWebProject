<nav class="navbar p-0 fixed-top d-flex flex-row">
    <div class="navbar-brand-wrapper d-flex d-lg-none align-items-center justify-content-center">
        <a class="navbar-brand brand-logo-mini" href="/Home/Index">
            <img src="~/corona-free-dark-bootstrap-admin-template-1.0.0/template/assets/images/minilogot.png" alt="TMLogo" style="width: auto; height: 30px;" />
        </a>
    </div>
    <div class="navbar-menu-wrapper flex-grow d-flex align-items-stretch">
        <button class="navbar-toggler navbar-toggler align-self-center" type="button" data-toggle="minimize">
            <span class="mdi mdi-menu"></span>
        </button>

        <ul class="navbar-nav navbar-nav-right">
            <li class="nav-item dropdown d-none d-lg-block">
                <a class="nav-link btn btn-success create-new-button" id="addCommentDropdown" data-toggle="dropdown" aria-expanded="false" href="#">+ Yorum Ekle</a>
                <div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="addCommentDropdown">
                    <h6 class="p-3 mb-0">Yorum İşlemleri</h6>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item preview-item" href="@Url.Action("Create", "Comment")">
                        <div class="preview-thumbnail">
                            <div class="preview-icon bg-dark rounded-circle">
                                <i class="mdi mdi-comment-plus-outline text-primary"></i>
                            </div>
                        </div>
                        <div class="preview-item-content">
                            <p class="preview-subject ellipsis mb-1">Yeni Yorum Oluştur</p>
                        </div>
                    </a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item preview-item" href="@Url.Action("Index", "Comment")">
                        <div class="preview-thumbnail">
                            <div class="preview-icon bg-dark rounded-circle">
                                <i class="mdi mdi-comment-multiple-outline text-info"></i>
                            </div>
                        </div>
                        <div class="preview-item-content">
                            <p class="preview-subject ellipsis mb-1">Tüm Yorumları Görüntüle</p>
                        </div>
                    </a>
                    <div class="dropdown-divider"></div>
                    <p class="p-3 mb-0 text-center">Yorumları Yönet</p>
                </div>
            </li>

            <li class="nav-item dropdown border-left">
                <a class="nav-link count-indicator dropdown-toggle" id="messageDropdown" href="#" data-toggle="dropdown" aria-expanded="false">
                    <i class="mdi mdi-email"></i>
                    <span class="count bg-success">@(ViewBag.UnreadMessagesCount > 0 ? ViewBag.UnreadMessagesCount.ToString() : "")</span>
                </a>
                <div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="messageDropdown">
                    <h6 class="p-3 mb-0">Mesajlar</h6>
                    <div class="dropdown-divider"></div>
                    <p class="p-3 mb-0 text-center"><a href="@Url.Action("Index", "Message")">Tüm mesajları gör</a></p>
                </div>
            </li>

            <li class="nav-item dropdown">
                <a class="nav-link" id="profileDropdown" href="#" data-toggle="dropdown">
                    <div class="navbar-profile">
                        <img class="img-xs rounded-circle" src="~/corona-free-dark-bootstrap-admin-template-1.0.0/template/assets/images/adminIkon.jpg" alt="">
                        @if (User.Identity.IsAuthenticated && !string.IsNullOrEmpty(User.Identity.Name))
                        {
                            <p class="mb-0 d-none d-sm-block navbar-profile-name">
                                @User.Identity.Name
                            </p>
                        }
                        <i class="mdi mdi-menu-down d-none d-sm-block"></i>
                    </div>
                </a>
                <div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="profileDropdown">
                    <h6 class="p-3 mb-0">Profil</h6>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item preview-item" href="/Settings/Index">
                        <div class="preview-thumbnail">
                            <div class="preview-icon bg-dark rounded-circle">
                                <i class="mdi mdi-settings text-success"></i>
                            </div>
                        </div>
                        <div class="preview-item-content">
                            <p class="preview-subject mb-1">Ayarlar</p>
                        </div>
                    </a>
                    <div class="dropdown-divider"></div>
                    <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="/Identity/Account/Login" method="post">
                        <button type="submit" class="dropdown-item preview-item btn-link" style="width:100%; text-align:left; border:none; background:none;">
                            <div class="preview-thumbnail">
                                <div class="preview-icon bg-dark rounded-circle">
                                    <i class="mdi mdi-logout text-danger"></i>
                                </div>
                            </div>
                            <div class="preview-item-content">
                                <p class="preview-subject mb-1">Çıkış Yap</p>
                            </div>
                        </button>
                    </form>
                    <div class="dropdown-divider"></div>
                    <p class="p-3 mb-0 text-center">Gelişmiş Ayarlar</p>
                </div>
            </li>
        </ul>
        <button class="navbar-toggler navbar-toggler-right d-lg-none align-self-center" type="button" data-toggle="offcanvas">
            <span class="mdi mdi-format-line-spacing"></span>
        </button>
    </div>
</nav>