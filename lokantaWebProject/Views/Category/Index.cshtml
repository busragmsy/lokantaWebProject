@using lokantaWebProject.Entities
@model IEnumerable<Category>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var count = 1;
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Categories</h4>
            <p class="card-description">Manage your food categories here.</p>

            <a class="btn btn-success mb-3" href="@Url.Action("Create", "Category")">
                <i class="fa fa-plus"></i> Add New Category
            </a>

            <div class="table-responsive">
                <table class="table table-bordered table-striped">
                    <thead class="table-dark">
                        <tr>
                            <th>No</th>
                            <th>Category Name</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@count</td>
                                <td>@item.Name</td>
                                <td>
                                    <a class="btn btn-sm btn-primary" href="@Url.Action("Edit", "Category", new { id = item.Id })">
                                        <i class="fa fa-edit"></i> Edit
                                    </a>
                                    <a class="btn btn-sm btn-info" href="@Url.Action("Details", "Category", new { id = item.Id })">
                                        <i class="fa fa-eye"></i> Details
                                    </a>
                                    <a class="btn btn-sm btn-danger" href="@Url.Action("Delete", "Category", new { id = item.Id })" onclick="return confirm('Are you sure you want to delete this category?');">
                                        <i class="fa fa-trash"></i> Delete
                                    </a>
                                </td>
                            </tr>
                            count++;
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>


